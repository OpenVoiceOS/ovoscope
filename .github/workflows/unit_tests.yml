name: Run Tests
on:
  pull_request:
    branches:
      - dev
    paths-ignore:
      - 'ovoscope/version.py'
      - '.github/**'
      - '.gitignore'
      - 'LICENSE'
      - 'CHANGELOG.md'
      - 'README.md'
      - 'docs/**'
  push:
    branches:
      - dev
    paths-ignore:
      - 'ovoscope/version.py'
      - '.github/**'
      - '.gitignore'
      - 'LICENSE'
      - 'CHANGELOG.md'
      - 'README.md'
      - 'docs/**'
  workflow_dispatch:

jobs:
  unit_tests:
    uses: OpenVoiceOS/gh-automations/.github/workflows/coverage.yml@dev
    with:
      python_version: "3.11"
      install_extras: "-e .[pydantic]"
      test_path: "test/unittests/"
      coverage_source: "ovoscope"
