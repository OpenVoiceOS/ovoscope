name: Stable Release
on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  publish_stable:
    if: github.actor != 'github-actions[bot]'
    uses: OpenVoiceOS/gh-automations/.github/workflows/publish-stable.yml@master
    secrets: inherit
    with:
      branch: 'master'
      version_file: 'ovoscope/version.py'
      publish_pypi: true
      sync_dev: true
      publish_release: true
